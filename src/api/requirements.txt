# Container runtime requirements (kept lean so the image builds quickly and reliably).
# Evaluation / red-teaming / notebook tooling live in requirements-dev.txt and are
# used by the eval scripts and CI, not the running API.
fastapi
fastapi[standard]
openai
requests
prompty[azure]==0.1.40
pandas
jsonlines
python-dotenv==1.0.1
azure-search-documents
gunicorn==23.0.0
validators
tenacity
marshmallow==3.23.2
aiohttp==3.10.11

# ---- Identity & Foundry (current generation, endpoint-based) ----
azure-identity
azure-ai-projects>=1.0.0
azure-ai-agents>=1.0.0
azure-ai-inference
azure-keyvault-secrets

# ---- Microsoft Agent Framework (multi-agent orchestration) ----
# Foundry chat client (FoundryChatClient) lives in agent-framework-foundry.
# Avoid the `agent-framework` meta package, which pulls every integration
# (incl. agent-framework-azure-ai-search) and causes pip resolver backtracking
# against azure-search-documents.
agent-framework-core
agent-framework-foundry

# ---- Observability (OpenTelemetry -> Application Insights) ----
opentelemetry-instrumentation-fastapi
azure-monitor-opentelemetry
